version: 2

sources:
  - name: hapi_fhir_source 
    schema: main            
    tables:
      - name: raw_patients  
        description: "Råa patientdata från HAPI FHIR"
        columns:
          - name: patient_id
            tests:
              - not_null

      - name: raw_encounters
        description: "Råa encounterdata från HAPI FHIR"
        columns:
          - name: encounter_id
            tests:
              - not_null
